# CLAUDE.md

Read `AGENTS.md` first — it is the binding rule set for every AI agent in this repository, including Claude. All protected-core boundaries, engineering rules, and stop-and-ask triggers defined there apply in full.

This repo is **PKM-Shop** — a LINE AI commerce + delivery-operations platform, cloned from and pivoted off MiraCare (which lives separately). Business rules live in `Ready.md`; the build plan + DoD tracker live in `docs/pkm-shop-line-commerce-plan.md`. Read in the order given by AGENTS.md §1. **Never invent business rules** — if `Ready.md` doesn't cover a case, stop and ask the owner.
